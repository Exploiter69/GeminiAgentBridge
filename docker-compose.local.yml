services:
  gemini-agent-bridge:
    build: .
    container_name: gemini-agent-bridge
    ports:
      - "8081:8081"
    volumes:
      - ./config.json:/app/config.json:ro
      - ./cookies.json:/app/cookies.json:ro
    restart: unless-stopped
